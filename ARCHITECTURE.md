# 📊 MoodFlix - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     http://localhost:3000                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Navbar     │  │ MoodSelector │  │  MovieCard   │         │
│  │  Component   │  │  Component   │  │  Component   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │     Home     │  │ MoodResults  │  │MovieDetails  │         │
│  │     Page     │  │     Page     │  │     Page     │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐        │
│  │           API Service Layer (api.js)               │        │
│  └────────────────────┬───────────────────────────────┘        │
└───────────────────────┼────────────────────────────────────────┘
                        │
                        │ HTTP Requests
                        │ (Proxy via Vite)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                  BACKEND (Node.js + Express)                     │
│                     http://localhost:5000                        │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐        │
│  │              Express Server (server.js)            │        │
│  └────────────────────┬───────────────────────────────┘        │
│                       │                                          │
│  ┌────────────────────▼───────────────────────────────┐        │
│  │            Routes (routes/movies.js)               │        │
│  └────────────────────┬───────────────────────────────┘        │
│                       │                                          │
│  ┌────────────────────▼───────────────────────────────┐        │
│  │    Controllers (controllers/moviesController.js)   │        │
│  └────────────────────┬───────────────────────────────┘        │
└───────────────────────┼────────────────────────────────────────┘
                        │
                        │ HTTPS Requests
                        │ (API Key in Headers)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    TMDB API (External)                           │
│              https://api.themoviedb.org/3                        │
│                                                                  │
│  • Movie Database                                               │
│  • Genre Information                                            │
│  • Movie Details                                                │
│  • Trending Data                                                │
│  • Search Functionality                                         │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Mood Selection Flow
```
User clicks mood
    ↓
MoodSelector component
    ↓
Navigate to /mood/:mood
    ↓
MoodResults page
    ↓
api.getMoviesByMood(mood)
    ↓
Backend: /api/movies/mood/:mood
    ↓
Controller maps mood to genres
    ↓
TMDB API: /discover/movie?with_genres=...
    ↓
Returns movie list
    ↓
Display MovieCard components
```

### 2. Search Flow
```
User types in search bar
    ↓
Navbar component
    ↓
Navigate to /search/:query
    ↓
SearchResults page
    ↓
api.searchMovies(query)
    ↓
Backend: /api/movies/search/:query
    ↓
TMDB API: /search/movie?query=...
    ↓
Returns search results
    ↓
Display MovieCard components
```

### 3. Movie Details Flow
```
User clicks movie card
    ↓
Navigate to /movie/:id
    ↓
MovieDetails page
    ↓
api.getMovieDetails(id)
    ↓
Backend: /api/movies/details/:id
    ↓
TMDB API: /movie/:id
    ↓
Returns detailed movie data
    ↓
Display full movie information
```

### 4. Trending Movies Flow
```
Home page loads
    ↓
useEffect hook
    ↓
api.getTrendingMovies()
    ↓
Backend: /api/movies/trending
    ↓
TMDB API: /trending/movie/week
    ↓
Returns trending movies
    ↓
Display MovieCard components
```

## Component Hierarchy

```
App
├── Navbar
│   └── Search Form
├── Routes
│   ├── Home
│   │   ├── MoodSelector
│   │   │   └── MoodButton (x6)
│   │   └── Trending Section
│   │       └── MovieCard (x6)
│   ├── MoodResults
│   │   └── MovieCard (multiple)
│   ├── SearchResults
│   │   └── MovieCard (multiple)
│   └── MovieDetails
│       ├── Backdrop Image
│       ├── Poster Image
│       └── Movie Information
└── Common Components
    ├── Loader
    └── ErrorMessage
```

## State Management

```
App Level:
├── darkMode (boolean)
└── Router state

Page Level:
├── movies (array)
├── loading (boolean)
├── error (string|null)
└── URL params (mood, query, id)

LocalStorage:
└── lastMood (string)
```

## API Endpoints Map

```
Frontend → Backend → TMDB

GET /mood/:mood
    → /api/movies/mood/:mood
        → /discover/movie?with_genres={genres}

GET /search/:query
    → /api/movies/search/:query
        → /search/movie?query={query}

GET /movie/:id
    → /api/movies/details/:id
        → /movie/{id}

GET /
    → /api/movies/trending
        → /trending/movie/week
```

## Technology Stack Layers

```
┌─────────────────────────────────────┐
│         Presentation Layer          │
│  React Components + Tailwind CSS    │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│         Application Layer           │
│   React Router + Custom Hooks       │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│          Service Layer              │
│        API Service (api.js)         │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│          Network Layer              │
│      Fetch API + Vite Proxy         │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│         Backend Server              │
│      Express.js + Middleware        │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│         Business Logic              │
│    Controllers + Route Handlers     │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│         External API                │
│          TMDB API v3                │
└─────────────────────────────────────┘
```

## File Dependencies

```
Frontend:
main.jsx
  └── App.jsx
      ├── Navbar.jsx
      │   └── api.js (search)
      ├── Home.jsx
      │   ├── MoodSelector.jsx
      │   ├── MovieCard.jsx
      │   ├── Loader.jsx
      │   ├── ErrorMessage.jsx
      │   └── api.js (trending)
      ├── MoodResults.jsx
      │   ├── MovieCard.jsx
      │   ├── Loader.jsx
      │   ├── ErrorMessage.jsx
      │   └── api.js (mood)
      ├── SearchResults.jsx
      │   ├── MovieCard.jsx
      │   ├── Loader.jsx
      │   ├── ErrorMessage.jsx
      │   └── api.js (search)
      └── MovieDetails.jsx
          ├── Loader.jsx
          ├── ErrorMessage.jsx
          └── api.js (details)

Backend:
server.js
  └── routes/movies.js
      └── controllers/moviesController.js
          └── TMDB API (node-fetch)
```

## Environment Configuration

```
Development:
├── Frontend: localhost:3000 (Vite dev server)
├── Backend: localhost:5000 (Express server)
└── API: api.themoviedb.org (TMDB)

Production:
├── Frontend: Static files (CDN/hosting)
├── Backend: Node.js server (cloud hosting)
└── API: api.themoviedb.org (TMDB)
```

---

This architecture ensures:
✓ Separation of concerns
✓ Scalability
✓ Maintainability
✓ Clean code structure
✓ Easy debugging
✓ Reusable components
